---
type: Mentar Visual Scaffold
title: Building and Combining Algebraic Expressions
description: Visual scaffold hints for translating words into expressions, combining two expressions, and writing expressions for perimeter/area — NOT equation-solving (see algebra_equations.md for that).
tags: [mathematics, algebra, expressions, visual-scaffold]
subject: mathematics
topic_keywords: [algebraic expression, algebraic expressions, expression, expressions, writing algebraic, combining algebraic, an algebraic expression]
timestamp: "2026-08-11T00:00:00Z"
---

When writing or explaining this question, use ONE of these visual structures. There is
NO equation to solve here and NO "=" balance — the task is building or combining an
expression, not isolating a variable (that's a different scaffold, algebra_equations.md).

**Word-to-expression table** — map each phrase piece to a symbol:
```
"3 more than"     ->   + 3
"twice a number"  ->   2 × (number)
"a number n"      ->   n
------------------------------------
"3 more than twice a number n"  ->  2n + 3
```

**Combining two expressions** — line them up and add matching terms:
```
  a  =  3x + 2
  b  =  5x + 1
------------------
a + b = 8x + 3        (add the x-terms: 3x+5x=8x; add the numbers: 2+1=3)
```

**Perimeter/area from a labelled shape**:
```
  width  = x
  length = x + 4

  PERIMETER = width + width + length + length
            = x + x + (x+4) + (x+4)
            = 4x + 8

  AREA = width × length
       = x × (x + 4)
```

**Guidelines for the question text:**
- The answer IS an expression (e.g. "4x + 8"), never a number and never an equation
  with "=" in it — the child types just the expression.
- Never ask the child to "expand"/"factorise"/"simplify" a SINGLE expression already
  shown in the question — any equivalent form (expanded or not) scores as correct, so
  that phrasing can't tell whether the child did anything. Only ask questions where the
  answer must be built from a word description or from combining separate pieces.
